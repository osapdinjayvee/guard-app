<resources>
    <!--
      Compose owns app theming (ui/theme/Theme.kt). This XML theme only styles the window
      before the first frame is drawn, so it deliberately carries no colors or widget styles.
    -->
    <style name="Theme.GuardApp" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
